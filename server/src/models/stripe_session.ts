export class StripeSession {
  id: string;
  url: string;
  userId: string;
  productId: string;
  amount: number;
  status: string;
  priceId: string;
  stripe_customerId: string;
  sessionId: string;
  subscriptionId: string;
}
